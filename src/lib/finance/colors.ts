/**
 * ألوان بنود الصرف المسموحة (v2.2 §B2).
 *
 * تعيش في وحدة عادية لا في ملف `"use server"`: ملفات إجراءات الخادم في Next لا يُسمح لها
 * بتصدير أي شيء غير دوال async. تصدير ثابت منها يُسقط شجرة مكوّنات العميل بالكامل عند
 * التحميل في المتصفّح — وهو خطأ لا تكشفه اختبارات الوحدة والتكامل لأنها تستورد الوحدة
 * مباشرةً في Node بلا قيود Next.
 *
 * قائمة مغلقة عمداً: لا نص لون حر من المستخدم، فلا سبيل لحقن CSS.
 */
export const ITEM_COLORS = ["أزرق", "أخضر", "كهرماني", "أحمر", "بنفسجي", "رمادي"] as const;

export type ItemColor = (typeof ITEM_COLORS)[number];
