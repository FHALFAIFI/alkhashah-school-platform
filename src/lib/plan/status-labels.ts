/**
 * تسميات الحالة المعروضة للإجراءات التي «تعتمد وتقفل» في خطوة واحدة.
 *
 * قرار المصطلحات (v2.3 §3, D-034): الفعل «اعتماد» والحالة «معتمد»/«معتمدة» —
 * استُبدل «اعتماد وإقفال» ومشتقاته في كل الواجهة، وسير العمل نفسه لم يتغير
 * (الاعتماد يجمّد الحزمة كما كان). إعادة الفتح تبقى «إعادة فتح بسبب موثق».
 *
 * هذه دوال عرض فقط: القيمة المخزَّنة في قاعدة البيانات تبقى «معتمد»/«معتمدة» دون تغيير
 * (لا تُعدَّل البيانات ولا مفاتيح الصلاحيات) — فقط النص الظاهر للمستخدم يُوحَّد.
 */

/** حالة البرنامج المعروضة — القيمة المخزَّنة «معتمد» تُعرض كما هي (D-034) */
export function programStatusLabel(status: string): string {
  return status;
}

/** حالة اللجنة المعروضة — القيمة المخزَّنة «معتمدة» تُعرض كما هي (D-034) */
export function committeeStatusLabel(status: string): string {
  return status;
}

/** نص زر إعادة الفتح الموحَّد لكل إجراءات الاعتماد */
export const REOPEN_LABEL = "إعادة فتح بسبب موثق";
